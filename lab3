#include <iostream>
#include <cmath>
#include <cstdlib>
#include <ctime>
#include <vector>
#include <string>
using namespace std;
int main()
{
    //zadanie 1
int arr[10]={};
srand(time(nullptr));
for (int i=0; i<10; i++){
arr[i]=rand()%21-10;
cout << arr[i]<<endl;}
cout << pow(arr[0],2)+pow(arr[1],2)+pow(arr[6],2)<<endl;

//zadanie 2
int n;
cout<<"vedite dlinu"<<endl;
cin>> n;
int *arr2=new int[n];
cin>> n;
if (n<=10)  
    cout <<"vedite drugoe chislo"<< endl;
else        
cout << " vedite chisla ";
for (int i = 0; i < n; ++i) cin >> arr2[i];
for (int i = 0; i < n; ++i){ cout << arr2[i] << endl; if(arr2[i]%2) arr2[i]*=2;}
for (int i = 0; i < n; ++i) cout << arr2[i] << endl;
int k1,k2;
int sum=0;
cout << "k1"<< endl;
cin>>k1;
cout<<"k2"<< endl;
cin>>k2;
for(int i=k1; i<=k2;++i) sum+=arr2[i];
cout<<"summa"<<sum<<endl;
delete [] arr2;
//zadanie3
int length;
int a;
cout <<"dlina massiva"<<endl;
cin>>length;

vector<int> vec1;
for (int i=0; i<length;++i){ cin >>a; vec1.push_back(9);}
for (int i=0; i<length;++i) cout<< vec1[i];
vec1.insert(vec1.end(),9);
for (int i=0; i< vec1.size();++i) cout<<vec1[i];
sum=0;
for(int i=0; i< vec1.size();++i) sum+= vec1[i];
cout<<sum<<endl;
int nomer=-1;
for(int i=0; i< vec1.size();++i) {if (vec1[i]<0) nomer=1; break;}
//zadanie 4

return 0;
} 
